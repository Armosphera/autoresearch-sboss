[project]
name = "autoresearch-sboss"
version = "0.1.0"
description = "Autonomous keep-or-revert loop for SBOSS sovereign business workflows"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = []
